{
    "scripts": {
        "start": "npx parcel index.html",
        "build": "rm -rf public && npx parcel build index.html && mkdir public && mv dist/* public/ && rm -rf dist/"
    },
    "dependencies": {
        "@types/d3": "^5.7.2",
        "d3": "^5.14.1",
        "jquery-appear-original": "^0.6.2",
        "throttle-debounce": "^2.1.0"
    },
    "devDependencies": {
        "parcel": "^2.8.2",
        "parcel-plugin-static-files-copy": "^2.2.1",
        "typescript": "^3.7.2"
    },
    "staticFiles": {
        "staticPath": "data"
    }
}
